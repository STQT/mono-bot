# Telegram Web App - Инструкция

## Описание

Веб-приложение для Telegram, которое позволяет пользователям:
- Просматривать свои баллы
- Видеть доступные подарки
- Запрашивать подарки (если хватает баллов)
- Отслеживать статус доставки заказов
- Подтверждать получение заказа или оставлять комментарий с просьбой позвонить

## Функционал

### 1. Просмотр баллов
- Отображается текущий баланс пользователя
- Обновляется в реальном времени

### 2. Доступные подарки
- Список всех активных подарков
- Показывает стоимость в баллах
- Визуально выделяет доступные подарки (если хватает баллов)
- При клике на доступный подарок создается запрос

### 3. Мои заказы
- Список всех запросов на подарки пользователя
- Отображает статус заказа (ожидает, одобрено, отклонено, выполнено)
- Показывает статус доставки (ожидает отправки, отправлено, доставлено)
- Даты запроса и обработки

### 4. Подтверждение получения
- Когда заказ доставлен, появляется кнопка "Подтвердить получение"
- Можно подтвердить получение или указать, что не получили
- Если не получили, можно оставить комментарий с просьбой позвонить
- Комментарий сохраняется и виден администратору

## API Endpoints

### GET `/api/webapp/user/?telegram_id=ID`
Получает данные пользователя

### GET `/api/webapp/gifts/`
Получает список активных подарков

### GET `/api/webapp/redemptions/?telegram_id=ID`
Получает список заказов пользователя

### POST `/api/webapp/request-gift/`
Создает запрос на получение подарка
```json
{
    "telegram_id": 123456789,
    "gift_id": 1
}
```

### POST `/api/webapp/confirm-delivery/`
Подтверждает получение заказа
```json
{
    "redemption_id": 1,
    "confirmed": true,
    "comment": "Получил заказ, спасибо!"
}
```

## Интеграция в бота

В главном меню бота добавлена кнопка "📱 Мои подарки", которая открывает Web App.

## Настройка для продакшена

1. Убедитесь, что ваш домен добавлен в `ALLOWED_HOSTS` в `settings.py`
2. Обновите URL Web App в `bot/bot.py` на ваш реальный домен
3. Настройте HTTPS (обязательно для Telegram Web App)
4. В продакшене рекомендуется добавить валидацию `initData` для безопасности

## Тестирование

Для тестирования без Telegram можно использовать параметр URL:
```
http://localhost:8000/api/webapp/?telegram_id=YOUR_TELEGRAM_ID
```

## Безопасность

В текущей реализации для разработки используется простой параметр `telegram_id`. 
В продакшене рекомендуется:
1. Валидировать `initData` через секретный ключ бота
2. Использовать HTTPS
3. Добавить rate limiting для API endpoints

